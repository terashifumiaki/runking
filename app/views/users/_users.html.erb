<!--フォロー／フォロワーの表示でもユーザの一覧表示をしたいので、一覧表示の部分を users/_users.html.erb というファイルに記述してパーシャルにしておきます。-->

<% if users.any? %>
  <ul class="list-group list-group-flush">
    <% users.each do |user| %>
      <li class="list-group-item">
        <div>
          <%= user.name %>
        </div>
        <div>
          <p><%= link_to 'プロフィール', user_path(user) %></p>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>